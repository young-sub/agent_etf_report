# Use Domain-Stable Operational Acceptance Names

Accepted. Planning milestone labels are not Agent TReport product or domain language, so public schemas, CLI flags, documentation, tests, and generated operational artifacts must not name acceptance fields after milestone labels. Native operational handoff acceptance uses domain-stable names such as `verified_operational_flow_acceptance` because the acceptance concept remains meaningful after the planning milestone changes.
